version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"   # comunicación entre apps
      - "15672:15672" # panel web
    environment:
      RABBITMQ_DEFAULT_USER: proyecto
      RABBITMQ_DEFAULT_PASS: losPerdidos123